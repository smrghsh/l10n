{
  "noise": {
    "syntax": "noise(scale, offset)",
    "description": "Generates a noise texture",
    "inputs": [
      { "scale": "scale of the noise" },
      { "offset": "offset speed of the noise" }
    ]
  }
}
